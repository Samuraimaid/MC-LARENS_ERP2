# PHASE 4: DUPLICATION GRAPH

## Overview

This document maps all **duplicated files, folders, and structures** across the repository, identifying:
1. Exact duplications (same content)
2. Functional duplications (same purpose, different location)
3. Migration leftovers
4. Redundant configurations
5. Split configurations (should be consolidated)

---

## Critical Duplication: frontend/frontend

### Structure Comparison

| Layer | Path | Status | Purpose | Size | Notes |
|-------|------|--------|---------|------|-------|
| **Active** | `frontend/` | ✅ CURRENT | Vite React app | 285.96MB | **LIVE** |
| **Duplicated** | `frontend/frontend/` | ❌ ARTIFACT | CRA residue | 49.21MB | **DEAD** |

### Content Comparison

| File | frontend/ | frontend/frontend/ | Status |
|------|-----------|-------------------|--------|
| `package.json` | ✅ CURRENT (0.2.0-beta.0) | ❌ OLD (version diff?) | **DUPLICATE** |
| `package-lock.json` | ✅ CURRENT | ❌ OLD | **DUPLICATE** |
| `node_modules/` | ✅ 223.49MB (current deps) | ❌ OLD deps snapshot | **DUPLICATE** |
| `src/` | ✅ 3.05MB (active) | ❌ NOT PRESENT | UNIDIRECTIONAL |
| `vite.config.js` | ✅ CURRENT | ❌ LIKELY OLD | **DUPLICATE** |
| `build/` | ✅ 5.82MB (current output) | ❌ old output | **DUPLICATE** |

### Verification: Content Same or Different?

**Need to verify**:
```bash
# Compare package.json versions
diff frontend/package.json frontend/frontend/package.json

# Check if node_modules are same deps version
ls frontend/node_modules/ | wc -l  # Expected: ~500+ packages
ls frontend/frontend/node_modules/ | wc -l  # Likely different count

# Check build outputs
diff <(ls -la frontend/build/) <(ls -la frontend/frontend/build/)
```

**Assumption**: Different versions (CRA vs Vite → not bit-identical)

---

## Secondary Duplications: Configuration Files

### Environment Configuration Duplication

| Config | Location | Purpose | Status |
|--------|----------|---------|--------|
| `.env.example` | root | Template | ✅ ONE SOURCE |
| `.env` (local) | root or ? | Runtime override | ⚠️ **GITIGNORED** |
| `.env.production` | root or ? | Prod env | ⚠️ **GITIGNORED** |
| `vite.config.js` | frontend/ | Vite build | ✅ ONE |
| `vite.config.ts` | frontend/frontend/ | OLD Vite? | ❌ **POSSIBLE DUP** |

**Finding**: Vite configs in nested folder likely outdated

### Docker Configuration

| Config | Location | Status | Notes |
|--------|----------|--------|-------|
| `frontend/Dockerfile` | frontend/ | ✅ CURRENT | Multi-stage, Vite |
| `backend/Dockerfile` | backend/ | ✅ CURRENT | Python, FastAPI |
| `docker-compose.yml` | root | ✅ CURRENT | Services: mongodb, backend, frontend |
| `docker-compose.frontend.yml` | root | ⚠️ **DUPLICATE** | Separate frontend-only compose? |

**Finding**: `docker-compose.frontend.yml` exists separately — verify if needed

```yaml
# Recommendation: Check if docker-compose.frontend.yml differs from
# the frontend service definition in docker-compose.yml
diff <(grep -A 20 "frontend:" docker-compose.yml) \
     <(cat docker-compose.frontend.yml)
```

---

## Regenerable Files (Present Multiple Times)

### node_modules Duplications

| Location | Size | Regenerable? | Notes |
|----------|------|--------------|-------|
| `frontend/node_modules/` | 223.49MB | ✅ YES | npm ci |
| `frontend/frontend/node_modules/` | ?MB | ✅ YES | Old snapshot |
| `.venv/` | 11.31MB | ✅ YES | pip install |
| `backend/venv/` | ?MB | Unclear | If exists |

**Total Regenerable**: ~234MB+ (if both node_modules + venv)

### Build Outputs (Can Regenerate)

| Location | Size | Regenerable? | Generated By | Status |
|----------|------|--------------|--------------|--------|
| `frontend/build/` | 5.82MB | ✅ YES | npm run build | ✅ CURRENT |
| `frontend/frontend/build/` | ~5-10MB | ✅ YES | Old npm run build | ❌ STALE |
| `frontend/dist/` | ? | ✅ YES | If exists | ⚠️ CHECK |

**Finding**: Multiple build outputs (should have one)

---

## Cache & Temporary Duplications (216 Folders Total)

### Identified Cache Types

```
Backend caches:
├── backend/.pytest_cache/           (pytest)
├── backend/.mypy_cache/             (mypy type checking)
├── backend/.tox/                    (tox testing)
├── backend/.coverage                (coverage reports)

Frontend caches:
├── frontend/.eslintcache            (eslint)
├── frontend/.parcel-cache/          (parcel bundler - old?)
├── frontend/.next/                  (Next.js - SHOULD NOT EXIST)
├── frontend/node_modules/.cache/    (npm meta-cache)
├── frontend/node_modules/.vite/     (vite cache)

Python pycache (multiple locations):
├── backend/__pycache__/             (0.77MB)
├── backend/core/__pycache__/
├── backend/routes/__pycache__/
├── backend/services/__pycache__/
├── backend/models/__pycache__/
├── backend/middlewares/__pycache__/
├── backend/api/__pycache__/
├── backend/db/__pycache__/
├── backend/tests/__pycache__/

Others (TBD):
├── .git/objects/                    (git internal cache)
├── vscode settings cache            (if exists in workspace)
```

**Total Count Estimate**: ~216 cache folders (across all above categories)

**Regenerability**: ✅ **ALL regenerable**

---

## Code Duplication (Functional)

### Formatters (Already Fixed in Phase 3)

| Formatter | Former Locations | Current Location | Status |
|-----------|------------------|------------------|--------|
| `formatPhone` | 5 files (duplicate) | `frontend/src/lib/formatters.js` | ✅ **CONSOLIDATED** |
| `formatCedula` | 5 files (duplicate) | `frontend/src/lib/formatters.js` | ✅ **CONSOLIDATED** |
| `formatRUC` | 5 files (duplicate) | `frontend/src/lib/formatters.js` | ✅ **CONSOLIDATED** |
| `formatChasis` | 5 files (duplicate) | `frontend/src/lib/formatters.js` | ✅ **CONSOLIDATED** |
| `formatPlateNumber` | 5 files (duplicate) | `frontend/src/lib/formatters.js` | ✅ **CONSOLIDATED** |

**Status**: ✅ **RESOLVED in Microphase 1A**

### Environment Configuration Duplication (Frontend)

| Location | Purpose | Status |
|----------|---------|--------|
| `frontend/src/lib/env.js` | Vite env reading | ✅ CURRENT |
| `frontend/public/env.js` | Runtime env injection | ✅ CURRENT |
| `.env.example` | Template | ✅ CURRENT |
| Docker env vars | Injected at build | ✅ CURRENT |

**Finding**: Multiple env sources (design), but working correctly

---

## Script Duplication

### Startup Scripts

| Script | Type | Purpose | Status |
|--------|------|---------|--------|
| `start-local.sh` | Bash | Local dev startup | ✅ ONE |
| `start-local.bat` | Batch (?) | Windows equiv? | ⚠️ **CHECK IF EXISTS** |
| `start-mundo.sh` | Bash | Docker startup | ✅ ONE |
| `start-mundo.bat` | Batch | Windows Docker equiv | ✅ ONE |
| `start-autoparts.sh` | Bash | ? | ⚠️ **UNCLEAR PURPOSE** |
| `start-autoparts.bat` | Batch | ? | ⚠️ **UNCLEAR PURPOSE** |

**Finding**: Possible naming confusion (mundo vs autoparts) — both needed?

### Stop Scripts

| Script | Platform | Purpose | Status |
|--------|----------|---------|--------|
| `stop-docker-desktop.bat` | Windows | Docker down | ✅ ONE |
| `stop-docker-desktop-full.bat` | Windows | Docker down (full clean) | ✅ ONE (variant) |

**Finding**: Two variants of stop (normal vs full) — acceptable

### Health Check Scripts

| Script | Type | Purpose | Status |
|--------|------|---------|--------|
| `daily-login-healthcheck.bat` | Batch | Login validation | ✅ ONE |
| `daily_login_healthcheck.ps1` | PowerShell | Same purpose? | ⚠️ **DUPLICATE** |
| `scripts/daily_login_healthcheck.ps1` | PowerShell | Same purpose? | ⚠️ **DUPLICATE** |

**Finding**: Possible duplication of health check in different locations/languages

---

## Configuration File Duplication

### Babel & ESLint

| File | Location | Purpose | Status |
|------|----------|---------|--------|
| `.babelrc` or `babel.config.js` | root or frontend/ | CRA babel config | ⚠️ **CHECK IF REMOVED** |
| `.eslintrc.js` or `.eslintrc.json` | frontend/ | ESLint config | ✅ ONE |
| `.eslintignore` | frontend/ | ESLint exclusions | ✅ ONE (if exists) |

**Finding**: May have old CRA babel config (should be gone after Vite migration)

### Testing Configurations

| File | Location | Purpose | Current? |
|------|----------|---------|----------|
| `pytest.ini` | root | Pytest config | ✅ YES |
| `pytest_noaddopts.ini` | backend/ | Pytest variant | ⚠️ **DUPLICATE** |
| `pytest-minimal.ini` | root | Pytest variant | ⚠️ **DUPLICATE** |
| `playwright.config.js` | frontend/ | Playwright e2e | ✅ YES |

**Finding**: Multiple pytest configs (3 variants) — consolidate to ONE

### Lint Configurations

| File | Purpose | Status |
|------|---------|--------|
| `.prettierrc` | Code formatter | ✅ ONE |
| `prettier.config.js` | Same as above? | ⚠️ **CHECK** |
| `tailwind.config.js` | Tailwind CSS | ✅ ONE |
| `.stylelintrc` | CSS linter | ⚠️ **CHECK IF EXISTS** |

---

## Dependency File Duplication

### Python Requirements

| File | Location | Purpose | Status |
|------|----------|---------|--------|
| `requirements.txt` | backend/ | All deps | ✅ ONE |
| `requirements.prod.txt` | backend/ | Prod only | ✅ ONE (variant) |
| `requirements.local.txt` | backend/ | Local only | ✅ ONE (variant) |
| `requirements.dev.txt` | backend/ | Dev only | ⚠️ **CHECK** |

**Finding**: Good variance (prod vs local), but verify no duplication within variants

### NPM Dependencies

| File | Location | Status |
|------|----------|--------|
| `package.json` | frontend/ | ✅ ONE |
| `package.json` | frontend/frontend/ | ❌ **DUPLICATE** (to delete) |
| `package-lock.json` | frontend/ | ✅ ONE |
| `package-lock.json` | frontend/frontend/ | ❌ **DUPLICATE** (to delete) |

---

## Documentation Duplication

### README Files

| File | Location | Purpose | Status |
|------|----------|---------|--------|
| `README.md` | root | Main documentation | ✅ ONE |
| `README_FRONTEND_RUN.md` | root | Frontend-specific | ✅ ONE |
| `frontend/README.md` | frontend/ | Frontend duplicate? | ⚠️ **CHECK CONTENT** |
| `INSTALACION_LOCAL.md` | root | Setup instructions | ✅ ONE |

**Finding**: Check if `frontend/README.md` duplicates root README

### Release & Migration Notes

| File | Purpose | Status |
|------|---------|--------|
| `RELEASE.md` | Release notes | ✅ ONE |
| `PR_MIGRATION_NOTE.md` | Migration tracker | ✅ ONE |
| `MIGRATION_NOTES.md` | Possible variant? | ⚠️ **CHECK** |

---

## Duplication Summary Table

| Category | Count | Regenerable? | Action |
|----------|-------|--------------|--------|
| **Critical artifacts** | 1 | ✅ YES | Delete `frontend/frontend/` |
| **Node_modules copies** | 2+ | ✅ YES | Keep only active, delete old |
| **Build outputs** | 2+ | ✅ YES | Keep only `frontend/build/`, delete old |
| **Cache folders** | ~216 | ✅ YES | Automated cleanup script |
| **Python __pycache__** | ~10 | ✅ YES | Add `**/__pycache__/` to .gitignore |
| **Configuration variants** | 3-5 | ⚠️ MIXED | Consolidate pytest configs |
| **Script duplicates** | 2-3 | ⚠️ CHECK | Verify purpose (mundo vs autoparts) |
| **Dependency variants** | 3 | ✅ OK (intentional) | No action needed |

---

## Duplication Risk Matrix

| Duplication | Impact | Effort to Fix | Priority |
|-------------|--------|---------------|----------|
| `frontend/frontend/` | HIGH (49MB wasted) | LOW (delete folder) | 🔴 **CRITICAL** |
| `pytest*.ini` (3 files) | MEDIUM (confusion) | LOW (consolidate) | 🟡 **HIGH** |
| `node_modules` duplicates | HIGH (100MB+ waste) | LOW (.gitignore + clean) | 🟡 **HIGH** |
| `build/` duplicates | MEDIUM (10MB waste) | LOW (.gitignore) | 🟡 **MEDIUM** |
| Cache folders (216) | MEDIUM (10-50MB) | MEDIUM (script) | 🟡 **MEDIUM** |
| Script variants (mundo/autoparts) | LOW (minor confusion) | MEDIUM (decide purpose) | 🟢 **LOW** |

---

## Next Document: CLEANUP_SAFETY_MATRIX.md

Will classify every major folder/artifact with:
- **SAFE TO DELETE NOW** (confirmed regenerable)
- **SAFE AFTER BACKUP** (verified unused)
- **SAFE AFTER RUNTIME VALIDATION** (needs Docker/build test)
- **ARCHIVE INSTEAD** (move to /legacy_archive/)
- **DO NOT TOUCH** (critical)
