[pytest]
minversion = 7.0
addopts = -q --maxfail=1 --disable-warnings --strict-markers --cov=backend --cov-report=term --cov-report=xml:coverage.xml
testpaths = tests
python_files = test_*.py *_test.py
